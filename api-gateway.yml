zuul:
  prefix: /api
  ignoredServices: '*'
  routes:
    elf-activity-service: /api/v1/activities/**
    elf-task-manager: /api/v1/tasks/**
    elf-graph-service: /api/v1/graph/**
    elf-user-service: /api/v1/users/**
    elf-api-gateway: /gateway/**

server:
  port: 8080
  compression:
    enabled: true
    mime-types: application/json,text/css,application/javascript
    min-response-size: 2048

# Internationalization
spring.messages.basename: messages/messages

---
spring:
  profiles: docker
  zipkin:
    baseUrl: http://tracing-server:9411
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/
